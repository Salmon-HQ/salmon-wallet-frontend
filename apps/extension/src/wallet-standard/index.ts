/**
 * Wallet Standard adapter for the Salmon injected provider.
 *
 * Vendored from the `salmon-wallet-standard` npm package v0.0.6
 * (https://github.com/salmonw/salmon-wallet-standard, author "Salmon Developers",
 * Apache-2.0 — see LICENSE in this directory) so wallet-standard changes are
 * normal in-repo edits instead of an external publish. Only consumed by
 * `src/entrypoints/injected.ts`.
 */
export * from './initialize.js';
