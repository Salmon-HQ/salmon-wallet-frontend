appId: io.salmonwallet.app
---
# Onboard Wallet B from a clean install. Lands on Home with
# "Switch wallet account" visible.
- clearState
- launchApp
- runFlow: ./dev-launcher-pass.yaml
- runFlow: ./recover-walletB.yaml
- runFlow:
    when:
      visible:
        id: "biometric-skip-button"
    commands:
      - tapOn:
          id: "biometric-skip-button"
# First-run analytics consent is the final onboarding step. Decline to reach Home.
- runFlow: ./decline-analytics-consent.yaml
- extendedWaitUntil:
    visible: "Congratulations!"
    timeout: 60000
- tapOn:
    id: "success-go-to-wallet-button"
- extendedWaitUntil:
    visible: "Switch wallet account"
    timeout: 30000
