/**
 * Lives in `@salmon/shared` since spec 028 lot 6b — the extension runs the same
 * derivation. This module keeps the app's import path.
 */
export { useEnsureMirrorNetworks, useEnsureMirrorNetworks as default } from '@salmon/shared';
