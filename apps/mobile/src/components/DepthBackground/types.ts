import type { ViewStyle } from 'react-native';
import type { DepthBackgroundPropsBase } from '@salmon/shared';

/** Props for the DepthBackground component (React Native) */
export type DepthBackgroundProps = DepthBackgroundPropsBase<ViewStyle>;
