import type { ViewStyle } from 'react-native';
import type { FleshBackgroundPropsBase } from '@salmon/shared';

/** Props for the FleshBackground component (React Native) */
export type FleshBackgroundProps = FleshBackgroundPropsBase<ViewStyle>;
