import type { ViewStyle } from 'react-native';
import type { PendingActivityBannerPropsBase } from '@salmon/shared';

/** Props for PendingActivityBanner (React Native) — the shared contract plus a style. */
export type PendingActivityBannerProps = PendingActivityBannerPropsBase<ViewStyle>;
