import type { PendingValuePropsBase } from '@salmon/shared';
import type { ViewStyle } from 'react-native';

/**
 * Props for PendingValue (React Native)
 */
export interface PendingValueProps extends PendingValuePropsBase<ViewStyle> {}
