// The classifier lives in shared: the extension's paste field reads the same payloads.
export { classifyScanPayload } from '@salmon/shared';
export type { ScanClassification } from '@salmon/shared';
