import type { WalletInitErrorScreenPropsBase } from '@salmon/shared';

/** Props for WalletInitErrorScreen (React Native) — the shared contract, unchanged. */
export type WalletInitErrorScreenProps = WalletInitErrorScreenPropsBase;
